{
    "customDatasources": {
        "apache-nifi": {
            "defaultRegistryUrlTemplate": "http://localhost:3000/nifi-latest-version",
            "transformTemplates": [
                "$.version"
            ]
        }
    },
    "customManagers": [
        {
            "customType": "regex",
            "fileMatch": [
                "^Dockerfile$"
            ],
            "matchStrings": [
                "NIFI_VERSION=(?<currentValue>\\d+\\.\\d+\\.\\d+)"
            ],
            "datasourceTemplate": "apache-nifi",
            "depNameTemplate": "apache/nifi",
            "versioningTemplate": "semver"
        }
    ],
    "postUpgradeTasks": {
        "fileFilters": [
            "Dockerfile"
        ],
        "commands": [
            "./update-checksum.sh"
        ],
        "executionMode": "update"
    },
    "enabledManagers": [
        "custom.regex"
    ]
}
