{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "extends": [
        "config:recommended"
    ],
    "customDatasources": {
        "apache-nifi": {
            "defaultRegistryUrlTemplate": "http://127.0.0.1:8000/versions",
            "transformTemplates": []
        }
    },
    "customManagers": [
        {
            "customType": "regex",
            "fileMatch": [
                "^Dockerfile$"
            ],
            "matchStrings": [
                "ARG NIFI_VERSION=(?<currentValue>\\d+\\.\\d+\\.\\d+)"
            ],
            "datasourceTemplate": "custom.apache-nifi",
            "depNameTemplate": "apache-nifi"
        }
    ],
    "postUpgradeTasks": {
        "fileFilters": [
            "Dockerfile",
            "nifi-*-bin.zip.{asc,sha512}"
        ],
        "commands": [
            "./update-checksum.sh"
        ],
        "executionMode": "update"
    },
    "enabledManagers": [
        "custom.regex"
    ]
}
